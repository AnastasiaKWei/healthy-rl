One file per intervention draft: the file's whole text is the block body that lands
under "How to approach this:" on turn 1 (v2 semantics — sent once, not repeated).
Run it with:
    ./.venv/bin/python experiments/iterate.py --label <name> --block-file drafts/<name>.txt
